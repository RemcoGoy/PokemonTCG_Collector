import 'package:flutter/material.dart';

class PokeAppBar extends StatelessWidget{
  const PokeAppBar({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Pokemone collector"));
  }
  
}